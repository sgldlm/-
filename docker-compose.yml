services:
  polymarket-bot:
    build: ./backend
    container_name: polymarket-bot
    restart: unless-stopped
    ports:
      - "8787:8787"
    env_file:
      - ./.env
    volumes:
      - ./data:/app/data
